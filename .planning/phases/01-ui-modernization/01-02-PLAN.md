---
phase: 01-ui-modernization
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - frontend/src/views/Layout.vue
autonomous: true
requirements:
  - UI-01
  - UI-03
  - UI-04
  - UI-05
user_setup: []

must_haves:
  truths:
    - Navigation uses teal primary (#10B981) for accents and active states
    - Logo displays with primary color styling
    - Header has glassmorphism effect with white/alpha background
    - Top navigation menu items use hover transitions
    - Sidebar (admin) uses dark theme but with consistent color accents
    - Page content area has proper padding and background color
  artifacts:
    - path: frontend/src/views/Layout.vue
      provides: Modernized navigation layout with teal accents
      contains:
        - --color-primary: #10B981 in logo/icon styling
        - .header with backdrop-filter blur effect
        - el-menu items with teal hover/active colors
  key_links:
    - from: frontend/src/views/Layout.vue
      to: frontend/src/style.css
      via: CSS variables cascade
      pattern: "var(--color-primary)"
---

<objective>
Modernize Layout.vue with the health-focused teal color palette, refined typography, and smooth transitions. Apply color changes from blue (#409EFF) to teal (#10B981) while preserving the existing structure and glassmorphism header.

Purpose: Update the navigation layout to match the new design system
Output: Modernized Layout.vue with teal accents, consistent typography, smooth animations
</objective>

<context>
@frontend/src/views/Layout.vue
@frontend/src/style.css

# Design Decisions (locked):
- Primary: #10B981 (teal/emerald) - replaces current #409EFF blue
- Accent: #3B82F6 (blue)
- Text: #1E293B (primary), #64748B (secondary)
- Header: backdrop-filter blur, white/alpha background
- Transitions: 200-300ms ease
- Breakpoints: 1200px desktop, 768px tablet, 480px phone

# Current Layout.vue structure to preserve:
- .layout-container (flex, 100vh)
- .aside (admin sidebar, 240px, dark #001529)
- .main (flex column, contains header + content)
- .header (64px, sticky, glassmorphism)
- .top-nav (horizontal menu for regular users)
- .nav-logo (with Monitor icon)
- .content (flex: 1, padding 24px, scrollable)
- .fade-transform transitions (already exists)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modernize Layout.vue navigation colors and styling</name>
  <files>frontend/src/views/Layout.vue</files>
  <read_first>
    - frontend/src/views/Layout.vue
    - frontend/src/style.css
  </read_first>
  <action>
    Update frontend/src/views/Layout.vue with the following changes:

1. Replace ALL instances of #409EFF with var(--color-primary) or #10B981
   - Line ~256: .nav-logo color: #409EFF -> color: var(--color-primary)
   - Line ~277-278: .el-menu-item:hover color: #409EFF -> var(--color-primary)
   - Line ~281-282: .el-menu-item.is-active color: #409EFF -> var(--color-primary)
   - Line ~290-292: .el-sub-menu__title:hover color: #409EFF -> var(--color-primary)
   - Line ~228: .el-dropdown-link:hover color: #409EFF -> var(--color-primary)

2. Update .header styles (lines ~196-208):
   - Keep backdrop-filter: blur(10px)
   - Keep background: rgba(255, 255, 255, 0.95)
   - Change box-shadow to: 0 1px 3px rgba(0, 0, 0, 0.05)

3. Update .nav-logo (lines ~249-261):
   - Change color: #409EFF to: color: var(--color-primary)
   - Add font-weight: 600

4. Update .user-avatar hover effect (lines ~239-241):
   - Change transform: rotate(360deg) to: transform: scale(1.05)
   - This is more subtle and modern

5. Add responsive styles at the end of the <style scoped> section:
```css
/* Responsive navigation (D-18, D-19) */
@media (max-width: 768px) {
  .header {
    padding: 0 16px;
  }

  .nav-logo {
    font-size: 18px;
    margin-right: 16px;
  }

  .el-menu-horizontal {
    display: none;
  }

  .top-nav {
    flex-direction: row;
    justify-content: space-between;
  }
}

@media (max-width: 480px) {
  .header {
    padding: 0 12px;
  }

  .nav-logo .logo-text {
    display: none;
  }

  .user-info {
    margin-left: 8px;
  }
}
```

6. Update .fade-transform transitions for smoother feel (lines ~305-316):
   - Change transition duration from 0.4s to 0.2s
   - Change translateX values to translateY for fade-slide effect

7. Replace the transition name in template (line ~106):
   - Change name="fade-transform" to name="fade-slide"

DO NOT change the structure of the template - only update colors, transitions, and add responsive styles.
  </action>
  <verify>
    <automated>grep -c "color-primary" frontend/src/views/Layout.vue && grep -c "var(--color-primary)" frontend/src/views/Layout.vue</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/views/Layout.vue contains: var(--color-primary) at least 5 times (replacing #409EFF)
    - frontend/src/views/Layout.vue contains: .fade-slide transition classes
    - frontend/src/views/Layout.vue contains: @media (max-width: 768px)
    - frontend/src/views/Layout.vue contains: @media (max-width: 480px)
    - frontend/src/views/Layout.vue contains: .nav-logo with var(--color-primary)
    - frontend/src/views/Layout.vue contains: backdrop-filter: blur
    - frontend/src/views/Layout.vue does NOT contain: #409EFF
    - The .user-avatar hover uses scale(1.05) not rotate
  </acceptance_criteria>
  <done>Layout.vue navigation modernized with teal accents, smooth transitions, and responsive breakpoints</done>
</task>

</tasks>

<verification>
- All #409EFF blue colors replaced with teal (#10B981 / var(--color-primary))
- Page transitions use fade-slide effect
- Responsive breakpoints added for tablet and phone
- Header retains glassmorphism effect
</verification>

<success_criteria>
- Navigation uses teal primary color throughout
- Page transitions are smooth (200ms)
- Layout is responsive at 768px and 480px breakpoints
- User avatar hover is subtle scale effect
</success_criteria>

<output>
After completion, create `.planning/phases/01-ui-modernization/01-02-SUMMARY.md`
</output>
