---
phase: 03-analytics-record-management
plan: 03-04
type: checkpoint:human-verify
wave: 3
depends_on:
  - 03-01
  - 03-02
  - 03-03
files_modified: []
autonomous: false
requirements:
  - AN-01
  - AN-02
  - AN-03
  - AN-04
  - AN-05
  - DM-02
  - DM-03
user_setup: []
must_haves:
  truths:
    - "Analytics page displays correctly with all 4 metric tabs"
    - "Charts render with real data for 7-day and 30-day periods"
    - "Date range picker filters analytics correctly"
    - "Stats cards show correct avg/min/max/total values"
    - "View Analytics CTA on Home.vue navigates to analytics page"
  artifacts:
    - path: "frontend/src/views/Analytics.vue"
      provides: "Analytics page with charts and stats"
    - path: "backend/src/main/java/com/healthy/controller/AnalyticsController.java"
      provides: "Analytics REST endpoints"
---

<objective>
Human verification of Phase 03 implementation: Analytics page, trend charts, statistics, date range picker, and Home.vue navigation.
</objective>

<context>
@frontend/src/views/Analytics.vue
@backend/src/main/java/com/healthy/controller/AnalyticsController.java
@frontend/src/views/Home.vue
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify Analytics page and chart rendering</name>
  <what-built>
Complete Analytics.vue page with:
- 4 metric category tabs (身体指标/运动/饮食/睡眠)
- ECharts trend charts per category
- Period toggle pills (7天/30天)
- Date range picker (el-date-picker type="daterange")
- Stats cards showing avg/min/max/total
- Backend AnalyticsController with 4 REST endpoints
  </what-built>
  <how-to-verify>
**Step 1: Start the backend**
```bash
cd backend && mvn spring-boot:run
```
Wait for backend to start on port 9090.

**Step 2: Start the frontend**
```bash
cd frontend && npm run dev
```
Open http://localhost:5173 in browser.

**Step 3: Navigate to Analytics page**
- Login to the app
- Click "查看分析" button on Home.vue chart cards OR directly navigate to http://localhost:5173/layout/analytics

**Step 4: Verify page structure**
- Page shows "健康数据分析" heading
- Date range picker visible above tabs
- 4 tabs visible: 身体指标, 运动, 饮食, 睡眠
- Default view shows last 7 days

**Step 5: Test period toggle**
- Click "30天" pill - chart should reload with 30-day data
- Click "7天" pill - chart should reload with 7-day data

**Step 6: Test date range picker**
- Select a custom date range (e.g., last 14 days)
- Chart and stats should update to match selected range

**Step 7: Test all 4 tabs**
- Click each tab and verify chart renders correctly
- indicator (身体指标): line chart with sage/terracotta colors
- sport (运动): bar chart with gradient
- diet (饮食): line chart with sage
- sleep (睡眠): line chart with sage

**Step 8: Verify stats cards**
- Stats row shows 4 cards: 平均, 最低, 最高, 总计
- Values update when changing date range or period

**Step 9: Verify Home.vue navigation**
- Go back to Home.vue
- Click "查看分析" button near chart cards
- Should navigate to /layout/analytics
  </how-to-verify>
  <resume-signal>
Type "approved" or describe issues found (be specific about which tab, feature, or behavior is broken).
  </resume-signal>
</task>

</tasks>

<verification>
Backend compiles and starts: `cd backend && mvn spring-boot:run` exits 0
Frontend compiles: `cd frontend && npm run build` exits 0
Human verifies all 8 verification steps pass
</verification>

<success_criteria>
- Analytics page accessible at /layout/analytics
- All 4 tabs render charts correctly
- Period toggle (7天/30天) works
- Date range picker works with custom dates
- Stats cards show avg/min/max/total
- Home.vue "查看分析" button navigates to analytics
- No console errors on page load
</success_criteria>

<output>
After completion, create `.planning/phases/03-analytics-record-management/03-04-SUMMARY.md`
</output>
