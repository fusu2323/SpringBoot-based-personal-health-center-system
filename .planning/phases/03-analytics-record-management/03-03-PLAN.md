---
phase: 03-analytics-record-management
plan: 03-03
type: execute
wave: 2
depends_on:
  - 03-02
files_modified:
  - frontend/src/router/index.js
  - frontend/src/views/Home.vue
autonomous: true
requirements:
  - AN-01
  - AN-02
  - AN-03
  - AN-04
  - AN-05
user_setup: []
must_haves:
  truths:
    - "Analytics page is accessible at /layout/analytics route"
    - "User can navigate to Analytics from Home.vue chart cards"
  artifacts:
    - path: "frontend/src/router/index.js"
      provides: "Analytics route registered"
      contains: "path: 'analytics'"
    - path: "frontend/src/views/Home.vue"
      provides: "View Analytics CTA button"
      contains: "查看分析"
  key_links:
    - from: "router/index.js"
      to: "Analytics.vue"
      via: "component import"
      pattern: "Analytics"
---

<objective>
Wire up the Analytics.vue page into the router and add "View Analytics" CTA button on Home.vue chart cards.
</objective>

<context>
@frontend/src/router/index.js
@frontend/src/views/Home.vue
@frontend/src/views/Analytics.vue
@.planning/phases/03-analytics-record-management/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add analytics route to router/index.js</name>
  <read_first>
frontend/src/router/index.js
  </read_first>
  <action>
Add the analytics route to the router's children array under the Layout route.

In `frontend/src/router/index.js`, find the Layout children array (around line 27) and add:

```javascript
{
  path: 'analytics',
  name: 'Analytics',
  component: () => import('../views/Analytics.vue')
}
```

Add it after the health-record route (line 51) and before forum (line 54), or in a logical position.

The route structure should be:
- Parent: `/layout`
- Child: `/layout/analytics` -> Analytics.vue

The existing layout at `frontend/src/views/Layout.vue` already has `<router-view />` which will render Analytics.vue when the route matches.
</action>
  <acceptance_criteria>
- router/index.js contains `path: 'analytics'` in the children array under Layout
- router/index.js imports Analytics.vue with dynamic import `() => import('../views/Analytics.vue')`
- The analytics route is a child of the `/layout` parent route
  </acceptance_criteria>
  <verify>
    <automated>grep "analytics" frontend/src/router/index.js && grep "Analytics.vue" frontend/src/router/index.js</automated>
  </verify>
  <done>Analytics route registered at /layout/analytics</done>
</task>

<task type="auto">
  <name>Task 2: Add View Analytics CTA to Home.vue chart cards</name>
  <read_first>
frontend/src/views/Home.vue
  </read_first>
  <action>
Add a "View Analytics" button to Home.vue chart cards section (the `#bp-chart` and `#sport-chart` area).

Find the charts-section in Home.vue (search for `charts-section` or `id="bp-chart"`). Add a "View Analytics" button that navigates to `/layout/analytics`.

The button should use the sage primary color from CSS variables:
- Background: var(--color-sage)
- Color: white
- Border-radius: 100px (--btn-radius)
- Padding: 10px 24px
- Font-size: 14px
- Hover: var(--color-sage-dark)

Use `router.push('/layout/analytics')` or `<router-link>` to navigate.

Reference: D-03 from 03-CONTEXT.md: "View Analytics" CTA button on Home.vue chart cards navigates to `/layout/analytics`.

Add the button near the chart cards, perhaps in the section header area:

```html
<div class="charts-section">
  <div class="section-header">
    <h3>健康趋势</h3>
    <button class="analytics-cta" @click="$router.push('/layout/analytics')">查看分析</button>
  </div>
  <!-- existing chart cards -->
</div>
```

Or add it as a separate button below the charts:

```html
<div class="view-analytics-wrapper">
  <button class="view-analytics-btn" @click="$router.push('/layout/analytics')">
    <span>查看分析</span>
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <path d="M5 12h14M12 5l7 7-7 7"/>
    </svg>
  </button>
</div>
```

Style `.view-analytics-btn`:
- display inline-flex, align-items center, gap 8px
- background var(--color-sage), color white
- border-radius 100px, padding 12px 28px
- font-size 14px, font-weight 500
- border none, cursor pointer
- transition: background var(--transition-fast)
- Hover: background var(--color-sage-dark)
</action>
  <acceptance_criteria>
- Home.vue contains "查看分析" text
- Home.vue has @click handler or router-link that navigates to /layout/analytics
- The CTA button is visible near the chart cards section
  </acceptance_criteria>
  <verify>
    <automated>grep "查看分析" frontend/src/views/Home.vue && grep "layout/analytics" frontend/src/views/Home.vue</automated>
  </verify>
  <done>Home.vue has View Analytics button that navigates to /layout/analytics</done>
</task>

</tasks>

<verification>
- Router contains analytics route
- Home.vue contains "查看分析" button with /layout/analytics navigation
- Navigating to /layout/analytics renders Analytics.vue
</verification>

<success_criteria>
- Analytics route registered at /layout/analytics
- Home.vue chart cards section has "View Analytics" CTA
- Clicking CTA navigates to Analytics page
</success_criteria>

<output>
After completion, create `.planning/phases/03-analytics-record-management/03-03-SUMMARY.md`
</output>
